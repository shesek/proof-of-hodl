extend layout

block content
  .container.homepage
    p #[strong HODL.voting] is a #[a(href='https://github.com/bitcoin/bips/blob/master/bip-0112.mediawiki') CSV]-based time-lock
      |  weighted voting system that gives provable long-term Bitcoin HODLers a way to express their opinion on bitcoin matters.
      |  See #[a(href="https://github.com/shesek/proof-of-hodl") the readme on GitHub] for more details.
    ul
      for question in questions
        li
          a(href="/"+question.slug) #{ question.text } 
          | &mdash; #{ round(+formatSatoshis(question.total_weight/144), 0.01) } #[abbr(title='Bitcoin Days Locked') BDL]
